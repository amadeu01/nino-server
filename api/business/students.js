/**
* Author: Carlos Eduardo Millani
* Student business
*/
var errors = require('./errors');

module.exports = {
	validateIdentity: function(user_id, role_id, target) {
		//To edit a student must be its guardian(?) or an educator of the same school with enough permissions
	}
}
