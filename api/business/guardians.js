/**
* Author: Carlos Eduardo Millani
* Guardian business
* Last to modified:
*/
var permissions = require('./permissions');
var errors = require('./errors');

module.exports = {
	validateIdentity: function(user_id, role_id, target) {
		//To manage a guardian for a baby must be an educator, on the same school and have permissions
	}
}
